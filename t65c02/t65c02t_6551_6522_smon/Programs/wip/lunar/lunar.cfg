MEMORY {
    ZP:     start = $0010, size = $0050, type = rw;
    BSS:    start = $0400, size = $0400, type = rw;
    RAM:    start = $1000, size = $1000, type = rw;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    BSS:      load = BSS, type = bss;
    CODE:     load = RAM, type = rw,  start = $1000;
}

MEMORY {
    ZP:     start = $0010, size = $0030, type = rw;
    RAM:    start = $1000, size = $1000, type = rw, file = %O;
}

SEGMENTS {
    ZEROPAGE: load = ZP, type = zp;
    CODE:     load = RAM, type = ro, start = $1000;
}
